# Генератор пакетов (Веха 3) — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать человеку способ получить готовый, валидный пакет вопросов без ручного набора JSON —
через skill Claude Code, который генерирует пакет в диалоге и пишет его на диск.

**Architecture:** Никакого нового рантайм-кода (сервер/клиент не меняются). Два артефакта: (1)
маленький CLI-скрипт `server/scripts/validate-pack.ts`, тонкая обёртка над уже существующим
`validatePack()` из `server/src/pack.ts`, для проверки сгенерированного файла из командной строки;
(2) `.claude/skills/pack-generator/SKILL.md` — процедура, которой Claude следует в диалоге:
уточняющие вопросы (если нужно) → генерация основного раунда и кандидатов финала по content-правилам
→ запись `packs/<slug>.json` → прогон через скрипт (1) → починка при ошибке → отчёт человеку.

**Tech Stack:** TypeScript + `tsx` (уже в `server/devDependencies`) для скрипта-валидатора; обычный
markdown + YAML-фронтматтер для skill-файла (тот же формат, что у `.claude/skills/svoya-igra-dev/`).

## Global Constraints

- Спека: [2026-08-09-pack-generator-design.md](../specs/2026-08-09-pack-generator-design.md).
- Тип вопроса в сгенерированных пакетах — **всегда** `'обычный'`. Не `'кот'`/`'аукцион'` (вехи 4/5).
- Никаких медиа-вопросов (картинки/аудио/видео) — это отдельная идея в `docs/ideas.md`, не веха 3.
- Никакой памяти/обучения на прошлых партиях — отдельная более поздняя веха.
- Пакет пишется **только** в новый файл `packs/<slug>.json`. `packs/current.json` никогда не
  трогается этим skill'ом (см. спеку, «Запись и проверка»).
- Стандартный размер основного раунда: 2 раунда × 5 тем × 5 вопросов, цены `100/200/300/400/500`.
- Минимум 10 тем-кандидатов в финале.
- id вопросов: `r{раунд}-{slug_темы}-{цена}` для обычных, `final-{slug_темы}` для финальных —
  конвенция уже существующего `packs/current.json`.

---

### Task 1: CLI-скрипт для проверки сгенерированного пакета

**Files:**

- Create: `server/scripts/validate-pack.ts`

**Interfaces:**

- Consumes: `validatePack(data: unknown): Pack` из `server/src/pack.js` (уже существует, не
  меняется в этом плане — сигнатура из `server/src/pack.ts`: кидает `Error` с человекочитаемым
  сообщением на невалидных данных, иначе возвращает типизированный `Pack`).
- Produces: CLI-команда `npx tsx scripts/validate-pack.ts <путь-к-файлу>`, запускаемая **из
  директории `server/`** (`npx` резолвит бинарник `tsx` из `server/node_modules/.bin`, которого нет
  в корне репозитория — из корня та же команда молча скачает posторонний `tsx` через npm вместо
  того, чтобы использовать уже установленную зависимость воркспейса). Путь к файлу — относительно
  `server/`, например `../packs/current.json` для пакета, лежащего в `packs/` в корне репозитория.
  Код возврата `0` и `OK: <путь> — валидный пакет (...)` в stdout на успехе; код возврата `1` и
  сообщение об ошибке в stderr на провале. Task 2 и Task 3 вызывают именно эту команду в этой форме.

- [ ] **Step 1: Установить зависимости воркспейса**

Этот план выполняется в git worktree, где `pnpm install` мог ещё не запускаться — без этого не
будет ни `tsx`, ни `typescript`, ни `eslint` в `server/node_modules`.

```bash
cd C:\Users\ib1te\Desktop\svoya_igra\.claude\worktrees\pack-generator
pnpm install
```

Expected: команда завершается без ошибок; появляется `server/node_modules/.bin/tsx`.

- [ ] **Step 2: Написать скрипт**

```typescript
// server/scripts/validate-pack.ts
//
// Тонкая CLI-обёртка над validatePack() — используется генератором пакетов
// (.claude/skills/pack-generator/) для проверки сгенерированного файла перед
// тем, как отдать его как результат. Сама валидация уже покрыта тестами в
// server/src/pack.test.ts — здесь проверяется только то, что этот скрипт
// правильно транслирует results/errors в человеко- и скрипто-читаемый вывод
// (код возврата), а не переоткрывает те же кейсы.
import { readFile } from 'node:fs/promises';
import { validatePack } from '../src/pack.js';

const path = process.argv[2];
if (!path) {
  console.error(
    'Использование (из директории server/): npx tsx scripts/validate-pack.ts <путь-к-файлу>',
  );
  process.exit(1);
}

const raw = await readFile(path, 'utf-8');
let parsed: unknown;
try {
  parsed = JSON.parse(raw);
} catch (err) {
  console.error(`${path}: не JSON — ${(err as Error).message}`);
  process.exit(1);
}

try {
  const pack = validatePack(parsed);
  const questionCount = pack.rounds.reduce(
    (sum, round) =>
      sum + round.themes.reduce((s, theme) => s + theme.questions.length, 0),
    0,
  );
  console.log(
    `OK: ${path} — валидный пакет ("${pack.title}", ${pack.rounds.length} раунд(ов), ` +
      `${questionCount} вопрос(ов), финал: ${pack.final ? pack.final.themes.length + ' тем' : 'нет'})`,
  );
} catch (err) {
  console.error(`${path}: невалидный пакет — ${(err as Error).message}`);
  process.exit(1);
}
```

- [ ] **Step 3: Проверить на заведомо невалидном файле**

```bash
cd C:\Users\ib1te\Desktop\svoya_igra\.claude\worktrees\pack-generator\server
echo '{"title": "Тест"}' > /tmp/broken-pack.json
npx tsx scripts/validate-pack.ts /tmp/broken-pack.json
echo "exit code: $?"
```

Expected: сообщение вида `/tmp/broken-pack.json: невалидный пакет — ...: должно быть строкой`
(или похожее, в зависимости от того, какое поле `validatePack` проверяет первым) в stderr, `exit
code: 1`.

- [ ] **Step 4: Проверить на заведомо валидном файле**

```bash
npx tsx scripts/validate-pack.ts ../packs/current.json
echo "exit code: $?"
```

Expected: `OK: ../packs/current.json — валидный пакет ("Общая эрудиция", 2 раунд(ов), 32
вопрос(ов), финал: 3 тем)` (точные числа зависят от реального содержимого `packs/current.json` на
момент проверки — сверить с фактическим выводом, не считать число `32` догмой), `exit code: 0`.

- [ ] **Step 5: Убрать временный файл, прогнать typecheck/lint**

`server/tsconfig.json` ограничен `include: ["src"]`, поэтому обычный `npx tsc --noEmit` (из
`server/`) не заходит в `scripts/` — типы для него нужно проверить отдельной командой с теми же
флагами, что и в `tsconfig.json` (`target: ES2023`, `module`/`moduleResolution: NodeNext`,
`strict: true`), не трогая сам `tsconfig.json` ради одного скрипта:

```bash
rm -f /tmp/broken-pack.json
npx tsc --noEmit --strict --target ES2023 --module NodeNext --moduleResolution NodeNext scripts/validate-pack.ts
npx eslint scripts/validate-pack.ts
```

Expected: обе команды завершаются без ошибок и без вывода замечаний.

- [ ] **Step 6: Commit**

Steps 3–5 остаются в директории `server/` — вернуться в корень репозитория перед коммитом
(`git add`/`git commit` работают с путями относительно текущей директории):

```bash
cd C:\Users\ib1te\Desktop\svoya_igra\.claude\worktrees\pack-generator
git add server/scripts/validate-pack.ts
git commit -m "feat: add a CLI wrapper around validatePack for the pack generator"
```

---

### Task 2: Skill `pack-generator`

**Files:**

- Create: `.claude/skills/pack-generator/SKILL.md`

**Interfaces:**

- Consumes: `npx tsx scripts/validate-pack.ts <путь>` из Task 1 (точная команда, вызывается из
  директории `server/`, путь к пакету — относительно неё).
- Produces: сам skill — вызывается как `/pack-generator <темы>` или естественным языком («сгенерируй
  пакет на темы ...»). Пишет `packs/<slug>.json`. Task 3 запускает эту процедуру вручную (глазами
  агента, выполняющего план) как приёмочную проверку.

- [ ] **Step 1: Написать `SKILL.md`**

```markdown
---
name: pack-generator
description: Генерирует готовый пакет вопросов для «Своей игры» (packs/*.json) по названным темам или по интересам присутствующих. Использовать, когда просят сделать/сгенерировать новый пакет вопросов, тему для игры, набор вопросов на вечер. Не использовать для правки уже существующего пакета вручную (просто редактируется JSON-файл текстовым редактором) и не для вопросов о том, как работает формат пакета (см. server/src/pack.ts и design.md).
---

# Генератор пакетов «Своей игры»

Пишет один готовый, проверенный `packs/<slug>.json` за один заход в диалоге. Дизайн:
[2026-08-09-pack-generator-design.md](../../../docs/superpowers/specs/2026-08-09-pack-generator-design.md).

## Пять инвариантов, которые нельзя нарушать

1. **`packs/current.json` не трогается никогда.** Это файл, который реально читает запущенный
   игровой сервер. Пишем только новый файл.
2. **`type` вопроса — всегда `'обычный'`.** Даже если тема сама напрашивается на «кота» или
   «аукцион» — движок пока не умеет играть эти типы (вехи 4/5), помеченный ими вопрос был бы
   нерабочим контентом.
3. **Никаких медиа-вопросов.** Ни картинок, ни аудио, ни видео, ни ссылок на файлы, которых не
   существует. Только текст.
4. **Никакой памяти о прошлых партиях.** Не пытаться угадать, что уже игралось раньше, не
   подстраиваться под историю — этот генератор её не знает и не должен делать вид, что знает.
5. **Каждый сгенерированный вопрос обязан пройти `validate-pack.ts` (см. Шаг 6) до того, как
   назвать пакет готовым.** Невалидный файл — не результат, а незавершённая работа.

## Шаг 1. Собрать темы

Человек называет темы основного раунда сразу (список, одной строкой) — используем как есть.

**Если тем не названо вообще** — не гадать. Задать пару обычных вопросов: кто будет играть, чем
эти люди увлекаются, — и вывести темы из ответов самостоятельно. Не звать для этого никакой
отдельный инструмент или skill — это часть этой процедуры.

**Если названо меньше 10 тем** (2 раунда × 5 тем — см. Шаг 2) — недостающие темы добрать
самостоятельно, смежные к уже названным. Не переспрашивать по каждой недостающей теме отдельно,
если общее направление уже понятно из того, что человек уже сказал.

**Сложность** — если человек не задал явно, подобрать её самостоятельно так, чтобы соответствовала
широкой, разносторонней компании (не «для профессионалов», не «только для детей»), см. Шаг 4.

## Шаг 2. Основной раунд — форма

Ровно **2 раунда**, в каждом **5 тем**, в каждой теме **5 вопросов** с ценами `100, 200, 300, 400,
500` по возрастанию. Итого 10 тем и 50 вопросов на пакет.

Распределить собранные (и при нехватке — добранные) темы по двум раундам поровну (по 5 в каждый).
Порядок распределения не важен — темы в раундах логически независимы друг от друга.

## Шаг 3. Основной раунд — вопросы

Для каждой темы — 5 вопросов, по одному на каждую цену `100–500`. Каждый вопрос:

- `id`: `r{номер_раунда}-{slug_темы}-{цена}`, например `r1-geo-100`. `slug_темы` — тема,
  сокращённая/транслитерированная в короткий (обычно 3–6 символов) латинский идентификатор без
  пробелов, например «География» → `geo`, «Мифология» → `myth`, «Кино 90-х» → `kino90`. Единственное
  жёсткое требование — уникальность **внутри этого пакета**, не глобальная стабильность между
  разными сгенерированными пакетами.
- `price`: число из `100, 200, 300, 400, 500` — то, к какой цене привязан этот вопрос в рамках темы.
- `text`: сам вопрос, на русском, без ответа внутри формулировки.
- `answer`: короткий, однозначный ответ.
- `comment` (необязательно): короткое пояснение к ответу, если оно реально добавляет ценности
  (неочевидная связь, частая ошибка) — не заполнять просто чтобы заполнить.
- `type`: всегда `'обычный'` (инвариант 2 выше).

## Шаг 4. Содержательные правила (не проверяются скриптом — только собственным суждением)

- **Цена = трудность, а не метка.** Вопрос за 100 — узнаваемый, отвечаемый с ходу большинством
  играющих. Вопрос за 500 — специфический, для знающих тему глубоко. Между ними — плавный рост, не
  скачок «всё лёгкое, потом всё изощрённое».
- **Без повторов внутри пакета.** Один и тот же факт или ответ не должен быть решением двух разных
  вопросов пакета, даже в разных темах.
- **Проверяемость.** Ответ должен быть один, короткий, не спорный («это не точно» — плохой ответ).
  Вопросы «на вкус» (любимый фильм и т.п.) не годятся для игры, где верность проверяется.
- Если задана явная сложность (Шаг 1) — придерживаться её вместо «широкой аудитории по умолчанию».

## Шаг 5. Финал

Финал уже полностью реализован в игре — правила самого отсева тем, ставок и судейства из
[2026-08-05-final-round-design.md](../../../docs/superpowers/specs/2026-08-05-final-round-design.md)
не меняются. Здесь нужно только заполнить `final.themes`.

**Минимум 10 тем-кандидатов.** Источник — сначала темы, уже использованные в основном раунде (Шаг
2), затем смежные к ним. Если по конкретной теме нет уверенной идеи хорошего финального вопроса —
**предложить варианты человеку** вместо того, чтобы всё равно сочинить вопрос похуже. Финальная тема
не обязана быть темой основного раунда буквально — «смежная» значит достаточно похожая, чтобы
логично смотреться рядом с остальными кандидатами.

Каждая тема в `final.themes`:

- `name`: название темы, видимое игрокам при отсеве — до того, как они узнают сам вопрос.
- `question.id`: `final-{slug_темы}`, например `final-myth`. Та же конвенция id, что и в Шаге 3.
- `question.text`/`question.answer`/`question.comment?`: как в основном раунде (Шаг 3), но обычно
  чуть труднее среднего — финальный вопрос по определению не должен быть тривиальным.

## Шаг 6. Запись и проверка

1. Собрать всё в объект, соответствующий `Pack` из `server/src/pack.ts`:
   `{ title, author, createdAt, rounds: [...], final: { themes: [...] } }`.
   - `title`: короткое название по смыслу тем (не «Пакет 1», а что-то содержательное).
   - `author`: `'сгенерировано pack-generator'`.
   - `createdAt`: дата генерации в формате `YYYY-MM-DD`.
2. Выбрать `<slug>` для имени файла — короткое, по-английски, по смыслу тем (например
   `sport-kino-nauka`). Записать в `packs/<slug>.json` (путь от корня репозитория).
3. Прогнать: `(cd server && npx tsx scripts/validate-pack.ts ../packs/<slug>.json)`.
   (Именно из `server/` — там установлена зависимость `tsx`; из корня репозитория та же команда не
   найдёт локальный `tsx` и попытается скачать посторонний через npm. Скобки — подоболочка, чтобы
   смена директории не осталась в текущей сессии после команды.)
   - Если провалилось — прочитать сообщение об ошибке, поправить файл, прогнать ещё раз. Повторять,
     пока не пройдёт. Не показывать человеку невалидный файл как «готово».
   - Если прошло — идти дальше.

## Шаг 7. Отчёт

Сказать человеку: путь к файлу, число раундов/тем/вопросов, короткий список тем (основной раунд +
финальные кандидаты), и явно напомнить, что `packs/current.json` не тронут — как переключить игру на
новый пакет, решается отдельно (см. дизайн-спеку, раздел «Запись и проверка» — выбор пакета в UI
ещё не реализован на момент написания этого skill'а).
```

- [ ] **Step 2: Проверить, что YAML-фронтматтер валиден**

```bash
cd C:\Users\ib1te\Desktop\svoya_igra\.claude\worktrees\pack-generator
node -e "
const fs = require('fs');
const content = fs.readFileSync('.claude/skills/pack-generator/SKILL.md', 'utf-8');
const match = content.match(/^---\n([\s\S]*?)\n---/);
if (!match) { console.error('FAIL: no frontmatter block found'); process.exit(1); }
const hasName = /^name: pack-generator$/m.test(match[1]);
const hasDescription = /^description: /m.test(match[1]);
if (!hasName || !hasDescription) { console.error('FAIL: missing name or description'); process.exit(1); }
console.log('OK: frontmatter has name + description');
"
```

Expected: `OK: frontmatter has name + description`.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/pack-generator/SKILL.md
git commit -m "feat: add the pack-generator skill (Milestone 3)"
```

---

### Task 3: Приёмочная проверка — прогнать skill по трём сценариям

Здесь исполнитель плана **сам выступает в роли Claude, следующего только что написанному
`SKILL.md`** — а не пишет код. Цель — вживую пройти процедуру три раза с разными вводными и
убедиться, что на выходе каждый раз валидный, осмысленный пакет. Если по ходу обнаруживается, что
процедура в `SKILL.md` неполна или двусмысленна — поправить `SKILL.md` (Task 2) прямо по ходу, не
дожидаясь отдельной задачи.

**Files:**

- Modify (при необходимости по итогам сценариев): `.claude/skills/pack-generator/SKILL.md`
- Temporary (создаются и удаляются в рамках задачи, не коммитятся): `packs/*.json` — тестовые файлы
  сценариев ниже.

**Interfaces:**

- Consumes: `.claude/skills/pack-generator/SKILL.md` (Task 2), `npx tsx scripts/validate-pack.ts
<путь>` из директории `server/` (Task 1).
- Produces: подтверждение (или список найденных и исправленных проблем) того, что skill работает на
  трёх реалистичных сценариях — финальный артефакт этой задачи не код, а зафиксированный в этом же
  файле плана (отметить чекбоксы ниже) результат проверки.

- [ ] **Step 1: Сценарий «темы названы явно»**

Выполнить процедуру `SKILL.md` для вводной: «сгенерируй пакет на темы: спорт, история кино, наука,
литература, музыка 80-х». Ожидаемо: skill сам добирает недостающие темы до 10 (из четырёх названных
плюс смежные), пишет `packs/test-scenario-1.json`.

Прогнать (подоболочка `(cd server && ...)`, чтобы смена директории не осталась в текущей сессии
после команды — см. Task 1):

```bash
(cd server && npx tsx scripts/validate-pack.ts ../packs/test-scenario-1.json)
```

Expected: `OK: ...` (exit 0). Дополнительно прочитать сам файл глазами — 2 раунда × 5 тем × 5
вопросов, цены по возрастанию `100..500` в каждой теме, минимум 10 тем в `final.themes`, ни одного
вопроса с `type`, отличным от `'обычный'`.

Убрать тестовый файл (из корня репозитория): `rm packs/test-scenario-1.json`.

- [ ] **Step 2: Сценарий «темы не названы»**

Выполнить процедуру для вводной: «сгенерируй пакет, темы не важны». Ожидаемо: skill задаёт пару
уточняющих вопросов о том, кто играет и чем увлекается — вместо того, чтобы молча придумать темы
или, наоборот, отказаться работать без тем. Дать в ответ на эти вопросы правдоподобную вымышленную
вводную (например: «играют друзья, один увлекается программированием, другой — историей, остальным
всё равно») и убедиться, что skill доводит генерацию до конца на основе этого, пишет
`packs/test-scenario-2.json`.

Прогнать (подоболочка, как в Step 1):

```bash
(cd server && npx tsx scripts/validate-pack.ts ../packs/test-scenario-2.json)
```

Expected: `OK: ...` (exit 0). Убрать файл (из корня репозитория): `rm packs/test-scenario-2.json`.

- [ ] **Step 3: Сценарий «тем меньше, чем нужно для заполнения»**

Выполнить процедуру для вводной: «сгенерируй пакет на темы: программирование, аниме» (2 темы вместо
10). Ожидаемо: skill добирает 8 недостающих тем сам, смежных к названным, **не** переспрашивая по
каждой теме отдельно (см. Шаг 1 `SKILL.md`), пишет `packs/test-scenario-3.json`.

Прогнать (подоболочка, как в Step 1):

```bash
(cd server && npx tsx scripts/validate-pack.ts ../packs/test-scenario-3.json)
```

Expected: `OK: ...` (exit 0), 10 тем в основном раунде (2 явно заданные + 8 добранных). Убрать файл
(из корня репозитория): `rm packs/test-scenario-3.json`.

- [ ] **Step 4: Зафиксировать результат и закоммитить правки (если были)**

Если по итогам шагов 1–3 `SKILL.md` не менялся — этот шаг не создаёт коммита, отметить чекбоксы
выше как выполненные и всё. Если менялся (нашлась неоднозначность/пробел в процедуре, поправили) —
закоммитить:

```bash
git add .claude/skills/pack-generator/SKILL.md
git commit -m "fix: clarify pack-generator scenarios found during dogfooding"
```

- [ ] **Step 5: Полный прогон проверок проекта**

```bash
cd C:\Users\ib1te\Desktop\svoya_igra\.claude\worktrees\pack-generator
pnpm typecheck && pnpm lint && pnpm test && pnpm build
```

Expected: всё зелёное — новый скрипт (Task 1) не должен ломать существующие сборку/тесты, а новых
автотестов эта веха не добавляет (генератор — не код, тестируется прогоном процедуры, см. спеку,
раздел «Тестирование»).

---

## После плана

Веха не закрыта, пока по ней не сыграна настоящая партия с живыми людьми (`svoya-igra-dev`, шаг 7)
— см. спеку, раздел «Тестирование»: этот же вечер может закрыть одновременно и веху 2 (финал со
ставками, ещё не сыгран живьём), и веху 3, если играть по-настоящему сгенерированным этим skill'ом
пакетом.
